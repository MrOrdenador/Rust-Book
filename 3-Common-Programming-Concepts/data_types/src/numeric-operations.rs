fn main(){
    let sum = 5 + 10;
    let diffrence = 95.5 - 4.3;
    let product = 4 * 30;
    let quotient = 56.7 / 32.2; // normal division
    let truncated = -5 / 3; // -5 divided by 3 isn't an integer, so it truncates to 0
    let remainder = 43 % 5;
}