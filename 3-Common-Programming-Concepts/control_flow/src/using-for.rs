fn main() {
  // Why is this language so good?????

  let a = [10, 20, 30, 40, 50];
  let mut index = 0;

  for element in a {
      println!("The value is: {element}");
  }

  for number in (1..4).rev() { // in range??? This language has reunited every single good thing from other languages!
      println!("{number}!");
  }
  println!("LIFTOFF!!!");
}